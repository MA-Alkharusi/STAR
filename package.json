{
  "name": "mynasaapp",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@iconify/icons-mdi": "^1.2.48",
    "@iconify/react": "^4.1.1",
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "bootstrap": "^5.3.2",
    "firebase": "^10.7.1",
    "framer-motion": "^10.16.4",
    "google-map-react": "^2.2.1",
    "i": "^0.3.7",
    "react": "^18.2.0",
    "react-bootstrap": "^2.9.1",
    "react-datepicker": "^4.24.0",
    "react-dom": "^18.2.0",
    "react-intersection-observer": "^9.5.3",
    "react-lottie": "^1.2.3",
    "react-router-dom": "^6.17.0",
    "react-scripts": "5.0.1",
    "styled-components": "^6.1.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "legacy": "npm install i react-lottie --legacy-peer-deps && npm install react-datepicker --legacy-peer-deps && npm install react-intersection-observer --legacy-peer-deps && npm install firebase --legacy-peer-deps && npm install react-bootstrap bootstrap --legacy-peer-deps && npm install styled-components --legacy-peer-deps && npm install @iconify/icons-mdi --legacy-peer-deps && npm install @iconify/react --legacy-peer-deps && npm install google-map-react --legacy-peer-deps && npm install --legacy-peer-deps"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
