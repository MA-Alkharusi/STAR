import planet_loading from './planet_loading.gif'

const Loader = () => {
    return (
        <div className="loader">
            <img src={planet_loading} alt="Loading" />
        </div>
    )
}

export default Loader